[project]
name = "manictime-api-wrapper"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1",
    "httpx>=0.28",
    "flask-caching>=2.3",
    "gunicorn>=23.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pylint>=3.0",
    "ruff>=0.11",
    "ty>=0.0.1a7",
]
