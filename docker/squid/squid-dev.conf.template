# Allowlisted destinations — set via ENV MT_HOSTNAME
acl allowed_domains dstdomain ${MT_HOSTNAME}

# Dev extras: allow package registries and Claude Code
acl allowed_domains dstdomain .anthropic.com
acl allowed_domains dstdomain .claude.ai
acl allowed_domains dstdomain .claude.com
acl allowed_domains dstdomain .pypi.org
acl allowed_domains dstdomain files.pythonhosted.org
acl allowed_domains dstdomain .github.com
acl allowed_domains dstdomain docs.manictime.com
acl allowed_domains dstdomain storage.googleapis.com

# Standard ports
acl SSL_ports port 443
acl Safe_ports port 80
acl Safe_ports port 443
acl Safe_ports port ${MT_PORT}
acl CONNECT method CONNECT

# Deny requests to unknown ports
http_access deny !Safe_ports
http_access deny CONNECT !SSL_ports

# Allow cache manager from localhost (used by healthcheck)
http_access allow manager localhost
http_access deny manager

# Allow whitelisted domains, deny everything else
http_access allow allowed_domains
http_access deny all

# Disable disk cache — this proxy is for traffic control only
cache deny all

# Proxy listening port
http_port 3128
